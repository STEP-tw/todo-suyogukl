<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <script src="/lib/todoList.js" charset="utf-8"></script>
    <link rel="stylesheet" href="/css/styleAllPage.css">
  </head>
  <body>
    <div class="main">
      <button class='deleteButton' id="${todo}" type="button" onclick="deleteTodo(this.id)" name="delete">deleteTodo</button>
      <h1 align="center">${title}</h1>
      <div class="todoItem">
        ${todoItem}
      </div>
      <div class="addItems">
        TodoItem :<input type="text" name="addItem" value="">
        <button type="button" id="${todo}" onclick="addItem(this.id)" name="button">addItem</button>
      </div>
      <div class="home">
        <a href="homePage"><<< Home</a>
      </div>
    </div>
  </body>
</html>
