<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <script src="/js/todoList.js" charset="utf-8"></script>
    <link rel="stylesheet" href="/css/styleAllPage.css">
  </head>
  <body>
    <div class="main">
  <a id="logout"href="/logout">Logout</a>
      <button class='deleteButton' id="${todo}" type="button" onclick="deleteTodo(this.id)" name="delete">deleteTodo</button>
      <h2 align="center"> <a id=""href="/homePage"><<<</a> ${title}</h2>
      <div class="todoItem">
        ${todoItem}
      </div>
      <div class="addItems">
        TodoItem :<input style="width:100px;height:25px;" type="text" name="addItem" value="">
        <button type="button" class="addButton" id="${todo}" onclick="addItem(this.id)" name="button">addItem</button>
      </div>
    </div>
  </body>
</html>
